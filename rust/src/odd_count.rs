pub fn odd_count(n: u64) -> u64 {
    // let mut arr=vec![];
    // for i in 1..n {
    //     if i % 2 == 1 {
    //         arr.push(i)
    //     }
    // }
    // return arr.len() as u64;
    n / 2
}
